//
// Created by davide on 7/1/19.
//

#include "cfs.hpp"
#include <cstdlib>
#include <unordered_set>

void ControlFlowStructure::build(const BasicBlock* , unsigned int )
{
    // first build a normal cfg.
//    AbstractBlock* mem = (AbstractBlock*)malloc(sizeof(AbstractBlock) * nodes);
    // visit nodes and rebuild the cfg as AbstractBlocks
//    std::unordered_set<unsigned int> visited;
}
