#![allow(clippy::upper_case_acronyms)] // I hate this >:(
#![allow(clippy::comparison_chain)] // this is ugly and completely unreadable
#![cfg_attr(docsrs, feature(doc_cfg))]

pub mod analysis;
pub mod disasm;
